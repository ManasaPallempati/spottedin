node_modules
dist
*.tsbuildinfo

# Local AI-history tooling and its output. history_organizer_output/ is ~57MB of
# private ChatGPT/Claude/Codex transcripts; this repo is public, and git history
# is permanent, so these must never be committed here.
history_organizer_output/
history_organizer.py
history_organizer_ui.py
launch_history_organizer_ui.bat
restore_codex_sessions.py
restore_codex_sessions.bat
